# Direct runtime dependencies for llm-honeypot (local-only detection pipeline).
# Compile with: uv pip compile --universal --python-version 3.10 --generate-hashes requirements.in -o requirements.txt
# NOTE: anthropic / Claude API arrives in Phase B via the ai-firewall library,
# not as a direct dependency here. The local pipeline imports none of it.
Flask
pydantic
python-dotenv
vaderSentiment
