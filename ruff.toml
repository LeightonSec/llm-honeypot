line-length = 100
target-version = "py310"

[lint]
select = ["E", "W", "F", "I", "N", "UP"]
ignore = ["E501"]

[lint.per-file-ignores]
# Test module groups helper imports beside the section that uses them; E402 there is intentional.
"test_hardening.py" = ["E402"]
